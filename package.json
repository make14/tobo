{
  "name": "glpaths",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "yarn": "^1.22.0",
    "netlify-lambda": "^0.4.0"
  },
  "dependencies": {
    "svg-path-to-polygons": "^0.1.0"
  },
  "scripts": {
    "build": "netlify-lambda build lambda"
  }
}
